services:
  - type: web
    name: aoa-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.x
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        value: mongodb+srv://wadme5884_db_user:nioqXXm5fdi6Zs8X@cluster0.yecbbmt.mongodb.net/
      - key: JWT_SECRET
        fromValue: ${JWT_SECRET}
      - key: FRONTEND_URL
        value: "*" # Cambiar por la URL real del frontend después del despliegue
